repositories {
  google()
  mavenCentral()
  flatDir{
    dirs 'src/main/libs', 'libs'
  }
}

dependencies {
  implementation(name:'tbxml-android', ext:'aar')
}

android {
  compileSdkVersion 28

  packagingOptions {
    exclude 'README'
    exclude 'LICENSE'
  }
}
